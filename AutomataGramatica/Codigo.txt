const m = suma, n = numero; 
var x, y, z, q, r;
procedure multiplicar;
	var a, b;

begin a := x; b := y; z := 0;
	while b>0 do
	begin
	  if odd b then z := z + a;
	  a := 2 * a; b := b/2
	end
end

begin
	if a >= b then
	a:=suma
end
.